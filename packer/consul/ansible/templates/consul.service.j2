[Unit]
Description=Consul Client
After=network.target

[Service]
ExecStart=/usr/local/bin/consul agent -config-file={{ consul_config_dir }}/consul.hcl
ExecReload=/bin/kill -9 $MAINPID
KillSignal=SIGTERM
Restart=on-failure
RestartSec=1

[Install]

WantedBy=default.target
